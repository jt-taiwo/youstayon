import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const background = Color(0xFF0E150F);
  static const surface = Color(0xFF1A211B);
  static const surfaceLow = Color(0xFF161D17);
  static const surfaceBright = Color(0xFF333B34);

  // Primary
  static const primary = Color(0xFF60F99E);
  static const primaryContainer = Color(0xFF3DDC84);
  static const primaryDark = Color(0xFF005C31);

  // Text
  static const textPrimary = Color(0xFFDCE5DB);
  static const textSecondary = Color(0xFFBBCBBC);

  // Border
  static const outline = Color(0xFF869587);
  static const outlineVariant = Color(0xFF3C4A3F);

  // Status
  static const success = Color(0xFF43E188);
  static const warning = Color(0xFFFFB175);
  static const error = Color(0xFFFF6B6B);

  // Radar
  static const radar = Color(0xFF60F99E);

  // Wallet Accent
  static const walletGlow = Color(0x803DDC84);

  // Glass
  static const glassWhite = Colors.white10;
  static const glassBorder = Colors.white12;
}